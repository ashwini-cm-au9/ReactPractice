
const heading=React.createElement('h1',{id:'heading',color:'red'},"This is the second Heading")

const root=ReactDOM.createRoot(document.getElementById('root'))

root.render(heading)

const JSX=<h1>This is JSX</h1>

root.render(JSX)
